services:
  pharmagenomics:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        OLLAMA_MODEL: ${OLLAMA_MODEL:-medgemma}
    ports:
      - "11434:11434"
    volumes:
      - ./output:/app/output
